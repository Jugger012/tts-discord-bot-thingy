# ─── Discord ──────────────────────────────────────────────────────────────────
py-cord>=2.6.0           # discord.py fork with voice sink support
PyNaCl>=1.5.0            # Opus encryption for Discord voice
davey>=0.1.6             # Discord DAVE end-to-end encryption for voice

# ─── LLM ──────────────────────────────────────────────────────────────────────
google-genai>=2.3.0      # Google Gemini API SDK (Interactions API)
openai>=1.40.0           # OpenAI API SDK (optional backend)

# ─── TTS ──────────────────────────────────────────────────────────────────────
coqui-tts>=0.27.0        # Maintained Coqui TTS fork with precompiled Windows wheels
torchcodec>=0.16.0       # Audio decoding for PyTorch/Coqui TTS (requires FFmpeg 7 shared DLLs)

# f5-tts                 # Uncomment for F5-TTS backend (install separately)
elevenlabs>=1.9.0        # ElevenLabs cloud TTS SDK
httpx>=0.27.0            # HTTP client for Cartesia REST API

# ─── STT ──────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.3    # Local Whisper inference (CTranslate2 backend)

# ─── VAD ──────────────────────────────────────────────────────────────────────
webrtcvad-wheels>=2.0.10 # WebRTC Voice Activity Detection (pre-compiled binary wheels for Windows)

# ─── Audio Processing ─────────────────────────────────────────────────────────
pydub>=0.25.1            # Audio manipulation (trimming, resampling, normalization)
noisereduce>=3.0.3       # Spectral noise reduction for reference audio cleaning
soundfile>=0.12.1        # Read/write audio files (WAV, FLAC, etc.)
numpy>=1.26.0            # Audio buffer math

# ─── Config ───────────────────────────────────────────────────────────────────
pydantic-settings>=2.3.0 # Settings management via Pydantic BaseSettings
python-dotenv>=1.0.1     # .env file loading

# ─── Async Utilities ──────────────────────────────────────────────────────────
aiofiles>=23.2.1         # Async file I/O

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
